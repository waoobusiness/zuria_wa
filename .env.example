# === Render / Service Node ===
PORT=3001

# Persistance sur disque Render
AUTH_DIR=/var/data/wa
MEDIA_DIR=/var/data/wa/media

# L’URL publique de ton service Render (sans / final)
PUBLIC_BASE_URL=https://zuria-wa.onrender.com

# L’URL de l’Edge Function Supabase (sans query)
SUPABASE_WEBHOOK_URL=https://xlumvfhlokzhsonadwio.supabase.co/functions/v1/whatsapp-webhook-gateway

# Secret GLOBAL pour signer UNIQUEMENT "session.created"
# Doit être IDENTIQUE à WA_WEBHOOK_SECRET (côté Supabase > Functions > Secrets)
WEBHOOK_SECRET=ZuriaWebhookSecret2025
