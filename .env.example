# Port HTTP écouté par Fastify dans Render
PORT=3001

# Dossier local (côté Render) où on sauvegarde les credentials chiffrés WhatsApp par session
AUTH_DIR=./.wa

# URL publique de ce service Render (sans slash final)
PUBLIC_BASE_URL=https://zuria-wa.onrender.com

# URL de l'Edge Function Supabase
# -> tu l'as déjà: https://xlumvfhlokzhsonadwio.supabase.co/functions/v1/whatsapp-webhook-gateway
SUPABASE_WEBHOOK_URL=https://xlumvfhlokzhsonadwio.supabase.co/functions/v1/whatsapp-webhook-gateway

# Secret GLOBAL partagé entre Render et Supabase.
# Ce DOIT être le même que WA_WEBHOOK_SECRET dans les "Secrets" Supabase Edge Functions.
WEBHOOK_SECRET=ZuriaWebhookSecret2025
